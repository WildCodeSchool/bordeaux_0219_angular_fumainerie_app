import { GouvernancePersonality } from './gouvernance-personality';

describe('GouvernancePersonality', () => {
  it('should create an instance', () => {
    expect(new GouvernancePersonality()).toBeTruthy();
  });
});
